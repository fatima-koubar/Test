⁸async ngOnInit() {
    if (this._integrationType == INTEGRATION_TYPES.PAYMENT_COMPONENT) {
      window.parent.postMessage({ action: 'ready' }, '*');
      window.addEventListener('message', async (event) => {
        if (event.data.action === 'sendPaymentMeanType' && event.data.paymentMeanType) {
          this.hateoasLinks.set(this.getPaymentMeanByWrapperId(event.data.paymentMeanType));
          this._document.body.classList.add('mono');
        } else {
          this.hateoasLinks.set(this.links());
        }
        await this.initiate();
      });
    } else {
      this.hateoasLinks.set(this.links());
      await this.initiate();
    }
  }


async ngOnInit(): Promise<void> {
  const isPaymentComponent =
    this._integrationType === INTEGRATION_TYPES.PAYMENT_COMPONENT;

  if (isPaymentComponent) {
    window.parent.postMessage({ action: 'ready' }, '*');

    window.addEventListener('message', this.handlePaymentMessage);
  } else {
    await this.initializeLinksAndStart();
  }
}

private handlePaymentMessage = async (event: MessageEvent): Promise<void> => {
  const { action, paymentMeanType } = event.data || {};

  const links =
    action === 'sendPaymentMeanType' && paymentMeanType
      ? this.getPaymentMeanByWrapperId(paymentMeanType)
      : this.links();

  this.hateoasLinks.set(links);

  if (paymentMeanType) {
    this._document.body.classList.add('mono');
  }

  await this.initiate();
};

private async initializeLinksAndStart(): Promise<void> {
  this.hateoasLinks.set(this.links());
  await this.initiate();
}
