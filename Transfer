I have this service and I want to add a unit test file for it

import { ApplePayAdapterService } from '../../apple-pay-adapter.service';

export class ApplePayLiveAdapter extends ApplePayAdapterService {
  session!: typeof window.ApplePaySession;

  setSession(session: typeof window.ApplePaySession): void {
    this.session = session;
  }

  startTransaction(): void {
    this.session.begin();
  }
}


and I have added new type 
ApplePaySession: {
      canMakePayments(): boolean;
      completePayment(status: number): void;
      begin(): void;
      abort(): void;

      onvalidatemerchant: (event: ApplePayValidateMerchantEvent) => void;
      completeMerchantValidation(merchantSession: MerchantSession | undefined): void;
      onpaymentauthorized: (event: ApplePayPaymentAuthorizationEvent) => void;
      applePayCapabilities(merchantIdentifier: string): Promise<PaymentCredentialStatusResponse>;
      oncancel: () => void;

      STATUS_SUCCESS: number;
      STATUS_FAILURE: number;
    };
