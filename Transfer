je veux optimiser ces deux bloc de codes: 

premier: this.translations = event.data.translations as TranslationsPayload;
            if (paymentMeanType) {
              paymentComponent.title = this.translations['iFrameTitle'] + ' ' + paymentMeanType;
            } else {
              paymentComponent.title = this.translations['iFrameTitle'];
            }


second: if (this.translations) {
      if (paymentMeanType) {
        iframePopin.title = this.translations['iFramePopinTitle'] + ' ' + paymentMeanType;
      } else {
        iframePopin.title = this.translations['iFramePopinTitle'];
      }
    }
