<mat-form-field>
            <mat-label for="paymentSettings" >Configurations</mat-label>
            <mat-chip-grid #chipGrid formControlName="paymentSettings">
              <mat-chip-row class="chip" *ngFor="let societe of pos_paymentSettings()" (removed)="remove(societe)">
                {{societe}}
                <button style="color: white;" matChipRemove aria-label="'remove ' + societe">
                  <mat-icon>cancel</mat-icon>
                </button>
              </mat-chip-row>
            </mat-chip-grid>
            <input 
              matInput
              required
              type="text"
              class="form-control"
              id="paymentSettings"
              name="paymentSettings"
              [matChipInputFor]="chipGrid"
              [matChipInputAddOnBlur]="addOnBlur"
              (matChipInputTokenEnd)="add($event)"/>
          </mat-form-field>


TypeError: Cannot read properties of undefined (reading 'focused')
