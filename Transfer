export class AppPaymentMeanComponent {
data = input.required<PaymentMeanData>();
readonly summaryFields = computed(() => {
    const { return_link, ...fields } = this.data();
 return fields;
  });

j'ai aussi ce commentaire pour le code ci-dessus: 

La suppression de la gestion explicite des erreurs dans la construction de summaryFields laisse le composant vulnérable à undefined si this.data() n'est pas encore défini. Contrairement à l'ancienne implémentation qui utilisait Object.entries(this.data()) dans ngOnInit, le computed s'exécute potentiellement avant que le signal data ne soit pleinement résolu, causant un échec de la réactivité du composant.

est-ce c'est pareil que le commentaire precedent ? 
