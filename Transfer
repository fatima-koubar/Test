checkout(): Observable<Redirection> {
    return this._applePayAdapter.checkout().pipe(
      switchMap((output) => {
        if(output) {
          this._applePaySessionBuilderService.putToken(output.token);
        }
        return of();
      }),
    );
  }
